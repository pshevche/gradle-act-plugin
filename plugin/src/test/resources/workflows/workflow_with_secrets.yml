name: Hello World
on: [ push ]

jobs:
  print_greeting_with_secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Greet someone
        run: echo "${{ secrets.GREETING }}, ${{ secrets.NAME }}!"
